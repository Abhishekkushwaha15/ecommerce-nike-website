# Component contracts

This directory is reserved for static component references and documentation.

The application’s runtime components live in `JS/components/`. Keep a component there when it is reused across more than one route; keep route-specific rendering and event handling in `pages/`.

Current shared runtime components include the site header and footer, product cards and grids, cart items, filter controls, the newsletter form, notifications, loading state, and interaction helpers.
